name: Build Flowframes

on:
  push:
    branches: [ "gh-releases" ]

jobs:
  build:
    strategy:
      matrix:
        configuration: [Release]
    runs-on: windows-latest 
    env:
      Solution_Name: Flowframes.sln
      Target: Flowframes
      Platform: x64

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    # Install the .NET Core workload
    - name: Install .NET Core
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x

    # Add  MSBuild to the PATH: https://github.com/microsoft/setup-msbuild
    - name: Setup MSBuild.exe
      uses: microsoft/setup-msbuild@v1.0.2

    # Restore the application to populate the obj folder with RuntimeIdentifiers
    - name: Build Flowframes
      run: |
        cd Code/
        nuget restore $env:Solution_name
        msbuild $env:Solution_Name /p:Configuration=$env:Configuration /p:Platform=$env:Platform -target:$env:Target
        cd ..
      env:
        Configuration: ${{ matrix.configuration }}

    - name: Packaging Flowframes
      run: |
        cd Code\bin\x64\Release
        mkdir "FlowframesApp9999"
        mkdir "FlowframesApp9999/FlowframesData"
        mkdir "FlowframesApp9999/FlowframesData/pkgs"
        cp "../../../../pkgs/av" "FlowframesApp9999/FlowframesData\pkgs\av"
        cp ../../../../pkgs/dain-ncnn FlowframesApp9999/FlowframesData\pkgs\dain-ncnn
        cp "../../../../pkgs/licenses" "FlowframesApp9999/FlowframesData\pkgs\licenses"
        cp "../../../../pkgs/rife-cuda" "FlowframesApp9999/FlowframesData\pkgs\rife-cuda"
        cp "../../../../pkgs/rife-ncnn" "FlowframesApp9999/FlowframesData\pkgs\rife-ncnn"
        cp "../../../../pkgs/flavr-cuda" "FlowframesApp9999/FlowframesData\pkgs\flavr-cuda"
        cp "../../../../pkgs/xvfi-cuda" "FlowframesApp9999/FlowframesData\pkgs\xvfi-cuda"
        cp "../../../../pkgs/rife-ncnn-vs" "FlowframesApp9999/FlowframesData\pkgs\rife-ncnn-vs"
        echo "9999" >> "FlowframesApp9999/FlowframesData/ver.ini"
        cp Flowframes.exe "FlowframesApp9999"
        cd ../../../../Build
        cp "../pkgs/py-amp" "..\Code\bin\x64\Release\FlowframesApp9999\FlowframesData\pkgs\py-amp"
        7za.exe a FF-9999-Full-RTX3000.7z -m0=flzma2 -mx7 "..\Code\bin\x64\Release\FlowframesApp9999"
